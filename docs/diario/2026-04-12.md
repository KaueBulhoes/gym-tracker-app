# 2026-04-12

## Refatoração: pasta própria para cada componente visual

### O que foi feito

- Movidas todas as telas para pastas próprias com `index.ts`:
  - `Auth/LoginScreen/`, `Auth/RegisterScreen/`
  - `Home/HomeScreen/`
  - `Workout/AddWorkoutPlanScreen/`, `AddWorkoutExercisesScreen/`, `WorkoutDayScreen/`, `ActiveWorkoutScreen/`
- Movidos todos os navegadores para pastas próprias com `index.ts`:
  - `navigation/AppNavigator/`, `AuthNavigator/`, `RootNavigator/`
- Atualizadas todas as importações relativas (`../../` → `../../../`)
- Atualizado CLAUDE.md: padrão de pasta própria agora vale para todo componente visual (telas, navegadores, componentes)
- Atualizados caminhos em todas as docs de telas e funcionalidades
