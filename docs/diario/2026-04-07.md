# 2026-04-07 — Início do Projeto

## O que foi feito

- Criado o CLAUDE.md com definições do projeto
- Definida a stack: React Native CLI + TypeScript + Supabase + Zustand
- Criada estrutura de documentação (Obsidian)

## Decisões tomadas

- React Native CLI ao invés de Expo (mais controle nativo)
- Supabase como backend (auth + database + sync multi-device)
- Obsidian/Markdown para documentação do processo

## Próximos passos

- Configurar ambiente de desenvolvimento (Node, JDK, Android Studio)
- Inicializar o projeto React Native
- Criar projeto no Supabase e configurar tabelas

---

## Sessão tarde — Setup Auth (Fases 1 e 2)

### O que foi feito

- Integração do `CLAUDE.md` com o Copilot via `.github/copilot-instructions.md`
- Criados prompts equivalentes ao `.claude/commands/` em `.github/prompts/`
- Instaladas dependências: `@supabase/supabase-js`, `react-native-url-polyfill`, `@react-native-async-storage/async-storage`, `@react-navigation/native`, `@react-navigation/native-stack`, `react-native-screens`, `zustand`
- Configurado client Supabase em `src/services/supabase.ts` com persistência via AsyncStorage
- Adicionado polyfill de URL em `index.js`
- Criado sistema de config local (`src/config.ts` gitignored, `src/config.example.ts` como template)
- Criada estrutura de navegação: `AuthNavigator`, `RootNavigator`, `navigation/types.ts`

### Decisões tomadas

- Variáveis de ambiente via `src/config.ts` (sem react-native-dotenv) — mais simples, sem dep extra
- `@react-navigation/native-stack` ao invés de `@react-navigation/stack` — performance nativa
- `RootNavigator` escuta `onAuthStateChange` diretamente do Supabase, sem store intermediária

### Próximos passos

- ~~Fase 3: `authService.ts` + `authStore.ts` (Zustand)~~ ✓
- ~~Fase 4: Componentes base `Button` e `Input`~~ ✓
- ~~Fase 5: Telas `LoginScreen` e `RegisterScreen`~~ ✓
- ~~Fase 6: Wiring no `App.tsx`~~ ✓

---

## Sessão noite — Implementação Auth (Fases 3–6) + Claude Code

### O que foi feito

- `authService.ts` — signIn, signUp, signOut, getSession via Supabase Auth
- `authStore.ts` — Zustand store com user, session, isLoading, error
- `Button.tsx` — componente com variantes primary/outline, loading state
- `Input.tsx` — componente com label, erro, toggle de visibilidade de senha
- `LoginScreen.tsx` — tela de login com email/senha
- `RegisterScreen.tsx` — tela de cadastro com validação de senhas
- `HomeScreen.tsx` — placeholder pós-login com botão de sair
- `App.tsx` conectado ao RootNavigator com NavigationContainer
- Fix: KeyboardAvoidingView no Android (behavior: height)
- Fix: Teclado começa minúsculo nos campos de senha
- Fix: Gradle AsyncStorage local_repo + local.properties
- Primeiro build Android rodando no celular físico (SM-M556B)
- Comando `/commit` criado para commits com registro automático de atividade

### Decisões tomadas

- Supabase Auth é suficiente (não precisa de Clerk/Auth0/Firebase Auth)
- Erros de auth propagados via throw → capturados no store → exibidos na UI
- Sem confirmação de email por enquanto (facilita desenvolvimento)

### Próximos passos

- ~~Criar tela Home real com lista de treinos~~ ✓
- Configurar login com Google (OAuth via Supabase)
- Criar funcionalidade de registro de treino

---

## Sessão noite 2 — HomeScreen completa + Design System

### O que foi feito

- Migração de todos os componentes de StyleSheet para styled-components
- Padrão de tratamento de erros (AppError + errorMapper + ADR-002)
- HomeScreen completa com:
  - Header com boas-vindas e nome do usuário
  - Meta semanal com barra de progresso e texto motivacional
  - Métricas: último treino + total do mês
  - Próximo treino com badge "Próximo" e botão "Começar Treino"
  - Seção "Meus Treinos" em accordion (inicia fechado) com lista completa
  - Treino próximo destacado com borda amarela e badge
- Componentes novos: Card (3 variantes), ProgressBar, WorkoutListItem
- Dados mockados em `src/mocks/homeData.ts` com tipos (Workout, WeeklyGoal, UserProfile)
- Instalação de react-native-vector-icons e react-native-linear-gradient
- Configuração de fontes de ícones no build.gradle Android

### Decisões tomadas

- styled-components/native ao invés de StyleSheet.create (ADR pendente)
- Dados mockados separados em `src/mocks/` para modelar DB futuramente
- LinearGradient removido temporariamente (crash BVLinearGradient) — header usa cor sólida
- Métricas reduzidas a 2 colunas (último treino + total do mês)

### Próximos passos

- Implementar navegação com Bottom Tabs
- Criar tela de registro de treino (iniciar/finalizar)
- Modelar tabelas no Supabase baseado nos tipos mockados
- Substituir dados mockados por dados reais do Supabase
