# 2026-04-11

## Persistência mock de treinos + HomeScreen com dados reais

### O que foi feito

- Criado `src/types/workout.ts` com tipos `Exercise`, `WorkoutDay`, `WorkoutPlan`
- Criado `src/stores/workoutStore.ts` (Zustand) com draft, planos salvos e último treino ativado
- Integrado `AddWorkoutPlanScreen` para inicializar o draft no store ao criar plano
- Integrado `WorkoutDayScreen` para ler/escrever exercícios via store (dados sobrevivem à navegação)
- Integrado `AddWorkoutExercisesScreen` para salvar o draft como plano, com contagem de exercícios por dia
- Botão "Salvar" movido do header para o final da lista de dias (full-width, desabilitado sem exercícios)
- Corrigido render loop (Maximum update depth exceeded) causado por `?? []` instável no selector do Zustand
- Reescrito `HomeScreen` para usar dados reais do store:
  - "Meus Treinos" só aparece se existem planos salvos, mostrando dias com exercícios
  - "Próximo Treino" só aparece após pressionar "Começar Treino"
  - "Começar Treino" ativa o primeiro dia do último plano salvo
  - Removidos mocks de treinos (`mockWorkoutPlan`, `mockNextWorkout`, `mockLastWorkout`)

### Sessão 2 — Tela de treino ativo, cargas e rodapé

- Criado `ActiveWorkoutScreen` com cronômetro hh:mm:ss, lista de exercícios e botão "Finalizar Treino"
- Modal de descanso com contagem regressiva mm:ss e auto-close
- Modal de carga por exercício com toggle "cargas diferentes por série" e inputs por série
- Botão "Finalizar" salva `WorkoutSession` no store (duração, cargas, timestamps)
- HomeScreen: removido card "Próximo Treino", accordion aberto por padrão
- HomeScreen: FAB substituído por bottom bar com 3 botões (Adicionar, Começar, Estatísticas)
- Botão "Começar" desabilitado quando não há planos
- Inputs numéricos em WorkoutDayScreen filtram caracteres não-numéricos
- Adicionados tipos `WorkoutSession` e `ExerciseWeight`
- Adicionada seção "Persistência de dados" no CLAUDE.md como padrão do projeto

### Sessão 3 — Exercícios como accordion com séries e checkboxes

- ActiveWorkoutScreen: exercícios viram accordions expandíveis com séries discriminadas
- Checkbox por série e por exercício, borda verde quando exercício concluído
- Ao marcar série como feita, timer de descanso inicia automaticamente
- Removido botão manual de descanso
- Botão de carga com label "Carga" embaixo para clareza
- Séries mostram carga individual: "Série 1 x 12 — 10 kg"

### Docs atualizadas

- `docs/telas/active-workout.md` — criado e atualizado
- `docs/funcionalidades/sessao-treino.md` — novo
- `docs/api/workout-store.md` — atualizado (sessions, finishWorkout)
- `docs/telas/index.md` — atualizado (mapa + tabela)
- `docs/funcionalidades/index.md` — atualizado
